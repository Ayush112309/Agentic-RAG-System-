# ============================================================
# Agentic RAG System — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# Python version: 3.10+
# ============================================================

# ── LangChain Core ──────────────────────────────────────────
langchain==0.2.16
langchain-core==0.2.38
langchain-community==0.2.16
langchain-openai==0.1.23

# ── LLM & Embeddings ────────────────────────────────────────
openai==1.43.0
sentence-transformers==3.0.1          # Local embedding fallback (no API key needed)
tiktoken==0.7.0                        # Token counting for OpenAI models

# ── Vector Database ─────────────────────────────────────────
chromadb==0.5.5                        # Local persistent vector store
# Alternative: faiss-cpu==1.8.0        # Uncomment if you prefer FAISS

# ── Document Loaders ────────────────────────────────────────
pypdf==4.3.1                           # PDF parsing (PyPDFLoader)
unstructured==0.15.1                   # Enhanced document parsing
python-docx==1.1.2                     # DOCX support (optional)

# ── Frontend ────────────────────────────────────────────────
streamlit==1.38.0

# ── Environment & Config ────────────────────────────────────
python-dotenv==1.0.1                   # .env file loading

# ── Utilities ───────────────────────────────────────────────
pydantic==2.8.2                        # Data validation
httpx==0.27.2                          # HTTP client (used internally by OpenAI)
numpy==1.26.4                          # Numerical operations (ChromaDB dep)
pandas==2.2.2                          # CSV processing
tqdm==4.66.5                           # Progress bars for batch operations

# ── Testing ─────────────────────────────────────────────────
pytest==8.3.2
pytest-cov==5.0.0

# ── Code Quality ────────────────────────────────────────────
black==24.8.0
isort==5.13.2
flake8==7.1.1